<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#F8F3E7"
    android:gravity="top">

    <!-- Tìm kiếm theo tên giáo viên -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Search by Email"
        android:textColor="#3E4A59"
        android:drawableLeft="@drawable/ic_search"
        android:textSize="14sp"
        android:layout_marginBottom="4dp" />
    <!-- AutoCompleteTextView for email search -->
    <AutoCompleteTextView
        android:id="@+id/autoCompleteTextViewSearchEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Search Email"
        android:padding="12dp"
        android:textColor="#2C3E50"
        android:background="@drawable/rounded_background"
        android:layout_marginBottom="16dp"
        android:textSize="16sp"/>

    <!-- TextView for "Customer List" title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Customer List"
        android:textColor="#2C3E50"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"
        android:gravity="center"/>

    <!-- ListView for displaying customers -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="16dp"
        android:elevation="4dp"
        android:radius="8dp">

        <ListView
            android:id="@+id/listViewCustomers"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:divider="@android:color/darker_gray"
            android:dividerHeight="1dp"
            android:background="@drawable/rounded_listview_background"/>

    </androidx.cardview.widget.CardView>

</LinearLayout>
